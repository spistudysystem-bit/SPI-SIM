import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.spisimulator.app',
  appName: 'Ultrasound Simulator',
  webDir: 'dist'
};

export default config;
